\section{Bit Manipulation}
   \subsection{Subset Operations}
      \includeCode{c++} {count subsets with give sum}{lhstyle} {count_subsets_with_given_sum.cpp}
      \includeCode{c++} {max xor of any subset of elements in the array}{lhstyle} {max_subset_xor.cpp}
      \includeCode{c++} {min xor of any subset}{lhstyle} {min_subset_xor.cpp}
      \includeCode{c++} {subset generation}{lhstyle} {subset_generation.cpp}
      \includeCode{c++} {check if subset of elements in the array sum up to k}{lhstyle} {subset_sum_check.cpp}
      \includeCode{c++} {max subset sum mod m}{lhstyle} {subset_with_max_sum_mod.cpp}
      \includeCode{c++} {iterate over all supersets represented by x}{lhstyle} {super_sets_iteration.cpp}
